# Clave de encriptacion Fernet (base64, 32 bytes).
# Genera una nueva con:  python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# URL del archivo fuente encriptado (.zip.enc)
DATA_SOURCE_URL=https://lsodeveloper.com/data/PyG_Anonimizado.zip.enc

